<div className="relative min-h-screen">
      {/* 1. THE NAVIGATION (Overlapping) */}
      {/* Fixed pins it to the top window, Absolute pins it to the top of this div */}
      <header className="fixed top-0 left-0 w-full z-50 bg-transparent">
        <MainNav />
      </header>

      {/* 2. THE BACKGROUND IMAGE (Starts at y=0) */}
      <div className="root-layout-bg min-h-[80vh] flex items-center justify-center">
         {/* This container now starts at the very top of the screen */}
         <UpperContent />
      </div>

      {/* 3. THE SCROLLING CARDS */}
      <main className="relative z-10 bg-[#f7f8f2] px-6 pb-20 rounded-t-[3rem] -mt-32 shadow-2xl">
         <div className="max-w-6xl mx-auto pt-12">
            <h2 className="text-3xl font-bold text-gray-800 mb-6">The Big Four</h2>
            <Outlet />
         </div>
      </main>
    </div>