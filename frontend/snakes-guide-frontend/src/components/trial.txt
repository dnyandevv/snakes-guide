import { Link, useLocation } from "react-router";

export default function UpperContent() {
    const location = useLocation();

    // Shared container classes to avoid repetition
    // Mobile: Full width, centered text, small margins
    // Desktop: 35% width, left-aligned, large specific margins
    const containerClasses = "w-full md:max-w-[45%] lg:max-w-[35%] px-6 md:px-0 md:ml-20 lg:ml-28 md:-mt-32 xl:-mt-52 text-center md:text-left";

    return (
        <div className="flex justify-center md:justify-start">
            {location.pathname === "/" && (
                <div className={containerClasses}>
                    <div className="transition-all duration-700 ease-out opacity-0 translate-y-4 animate-fadeIn">
                        <h1 className="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-4 leading-tight drop-shadow-lg">
                            Patterns Tell the Story. <br />
                            Read The Signs. Identify.
                        </h1>
                        <p className="text-lg sm:text-xl text-white/80 leading-relaxed drop-shadow-md">
                            Not every snake you see is harmful; some are shy,
                            some are curious, and all are fascinating.
                            <br className="hidden md:block" />
                            I&rsquo;ve always been captivated by these incredible creatures. 
                            This guide is my way of sharing that love.
                        </p>
                        <Link to="/snakes-info" className="inline-block bg-green-700 hover:bg-green-600 text-white font-semibold py-3 px-8 rounded-full mt-8 shadow-xl transition-all hover:scale-105 active:scale-95">
                            Explore More Species
                        </Link>
                    </div>
                </div>
            )}

            {location.pathname === "/snakes-info" && (
                <div className={containerClasses}>
                    <div className="transition-all duration-700 ease-out opacity-0 translate-y-4 animate-fadeIn">
                        <h1 className="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-4 leading-tight drop-shadow-lg">
                            Snakes Are Not Villains.<br />
                            They Are Survivors.
                        </h1>
                        <p className="text-lg sm:text-xl text-white/80 leading-relaxed drop-shadow-md">
                            Snakes have existed for over 100 million years.
                            <br />Some sense heat, some glide, and others play dead to escape danger.
                        </p>
                    </div>
                </div>
            )}
        </div>
    );
}
